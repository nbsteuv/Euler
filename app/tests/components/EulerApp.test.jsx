var expect = require('expect');

var EulerApp = require('EulerApp');

//TODO: Test that Exercise or Test components rendered on correct routing

describe('EulerApp', () => {
  it('should exist', () => {
    expect(EulerApp).toExist();
  });
});
