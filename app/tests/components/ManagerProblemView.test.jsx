var expect = require('expect');

var ManagerProblemView = require('ManagerProblemView');

describe('ManagerProblemView', () => {
  it('should exist', () => {
    expect(ManagerProblemView).toExist();
  });
});
