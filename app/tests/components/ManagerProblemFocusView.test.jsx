var expect = require('expect');

var ManagerProblemFocusView = require('ManagerProblemFocusView');

describe('ManagerProblemFocusView', () => {
  it('should exist', () => {
    expect(ManagerProblemFocusView).toExist();
  });
});
